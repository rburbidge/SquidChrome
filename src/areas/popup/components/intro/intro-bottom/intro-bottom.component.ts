import { Component } from "@angular/core";

/**
 * Layout component that contains a top container and bottom container. The bottom container is aligned to the bottom,
 * and the top container takes up the remaining height. Can be used with content projection.
 */
@Component({
    selector: 'intro-bottom',
    templateUrl: './intro-bottom.html',
    styleUrls: [ './intro-bottom.css' ]
})
export class IntroBottomComponent { }